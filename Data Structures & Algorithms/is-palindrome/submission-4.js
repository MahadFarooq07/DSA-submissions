class Solution {
    /**
     * @param {string} s
     * @return {boolean}
     */
    isPalindrome(s) {
        
        console.log(s.split(" ").reverse().join() )
        return s.split(" ").reverse().join("") = s.join(" ")
    }
}

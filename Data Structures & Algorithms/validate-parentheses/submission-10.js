class Solution {
    /**
     * @param {string} s
     * @return {boolean}
     */
    isValid(s) {

        let stack = []
        
        for(let c of s){

        if(c === "(" || c === "[" || c === "{"){
            stack.push(c)
        }


        }
    }
}
